# Current Work

This folder contains active feature specs and implementation notes.

Rules:
- One file per active feature.
- Keep acceptance criteria explicit.
- Keep a short running log at the bottom of each feature file.

Multiple active files are allowed when one feature is finishing while the next has already started.
When complete, move the file to `docs/work/completed/`.
